A simple graphing calculator written in Python. Currently this program only supports supports slope intercept form (y = mx + b). To use run with python (on linux arm64 systems can run the nuitka binary found in the releases.)
TODO:
    1. Add function to convert non slope intercept formulas to slope intercept formulas.
    2. Add in support inequalities.
    3. Add in support for quadratics.